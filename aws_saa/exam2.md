# Amazon AWS 資格取得のための演習問題集

<https://awsjp.com/exam/saa-sap/c/>

## あなたは Web システムの設計者です。このシステムはデータベースサーバとWebサーバの2階層から構成されています。共にVPC内のEC2インスタンスとRDSインスタンスとして構成されています。ユーザはWebサーバのみアクセスします。あるときインターネットからデータベースサーバへ不正なログインが試みられていることに気が付きました。これを防ぐためどのようにシステムの設計を変更すればよいでしょうか。2個選択してください

- ウ． データベースサーバ(RDS)をプライベートサブネットに移設する
- ✗: オ． VPC にパブリックサブネットを作成する。
- エ． VPC にプライベートサブネットを作成する。

## Amazon S3 内の Bucket に関して ARN名として正しいのはどれでしょうか

- イ． arn:aws:s3:::mybucketname

## Windows の EC2インスタンスを新規に作成しました。このインスタンスに対して PING を送信しても応答がありません。ただし同じアドレスでリモートデスクトップ接続は可能なため、ネットワークは問題ありません。Windows 側のファイアウォールは PING を応答する設定になっています。何の設定が必要でしょうか

- Windows インスタンスの場合、デフォルトのセキュリティグループのインバウンド(inbound)では「TCP 3389 (リモートデスクトップ)」のみ許可されています。PINGを受け取るには インバウンドの ICMP を許可します。

## EBS (Elastic Block Store) のボリュームを暗号化する方法は次の中のどれでしょうか

- EBS はデフォルトで暗号化されていません。暗号化するにはボリューム生成時に暗号化オプションを有効にします。

## わからん: ELB のレコードを point する ドメインの apex レコードはどのように記載されますか

## S3 の標準クラスはどこに自動的に複製されますか

- ア． リージョン内の複数のAZ(Availability Zone)
  - S3標準クラスでは自動的に同一リージョン内の他のAZに複製されます。

## S3の特徴として正しいものを全て選択してください

- イ． サーバ側の暗号化、あるいはアップロード前の暗号化などが可能。
- エ． S3 へのアクセスログを生成することが可能。
  - > オプションで、それに対して行われたすべてのリクエストのアクセスログレコードを作成するように Amazon S3 バケットを設定することができます。
- ア． HTTPS プロトコルを利用してアップロードやダウンロードが可能。
  - > HTTPS プロトコルを使用して、SSL エンドポイント経由で Amazon S3 にデータを安全にアップロードまたはダウンロードできます。セキュリティの強化が必要な場合には、サーバー側の暗号化 (SSE) オプションを使用して、保存されているデータを暗号化できます。受信ストレージ要求に暗号化情報が含まれない場合、保存する前にオブジェクトを自動的に暗号化するよう Amazon S3 バケットを設定できます。または、固有の暗号化ライブラリを使用して、Amazon S3 に保存する前にデータを暗号化することもできます。
- ウ． デフォルトでは作成時は作成者のみアクセスが可能。
  - > Amazon S3 はデフォルトでセキュリティに保護されています。作成時は、リソースの所有者のみがその作成した Amazon S3 リソースにアクセスできます。

## S3暗号化方式に関して、オブジェクトの暗号化と復号に Amazon S3 を利用しながら、オブジェクトの暗号化に使用するキーをユーザー側で管理する方式は以下のどれでしょうか

- ✗: イ． SSE-KMS
- ウ． SSE-C
  - SSE-S3・・・Amazon S3 がキーを管理。キーは定期的に更新される。
  - SSE-KMS・・・ユーザがKMSでキーを生成して管理。
  - SSE-C・・・ユーザーがキーを準備し管理。

## EC2にアタッチされている EBS(Elastic Block Store ) を 他の AZ にコピーする方法で一番適している方法はどれになりますか

- イ． EBS ボリュームのスナップショットを作成する。他のAZ からこのスナップショットにより新しいボリュームを作成する。

## あるAWSアカウントで AZ(ap-northeast-1a) にインスタンスを作成しました。また別の AWS アカウントで AZ(ap-northeast-1c) にインスタンスを作成しました。 このインスタンス間で100G レベルの通信を行いましたが通信料が課金されませんでした。考えられる理由は何でしょうか

- ✗: イ． 同一リージョン内のAZ 間の通信は課金されない。
- ア． 二つのインスタンスは物理的に同じ AZ に作成されていた。
  - AZの場所は非公開であり、アカウントAのus-east-1aとアカウントBのus-east-1aは同じ場所にあるとは限らない（場所を設定してしまうと必然的に1aに集中してしまうため）。アカウントAの1aとアカウントBの1cが同じ場所に設置される可能性もあり、その場合ネットワーク通信料が課金されなかったりコピーが高速になったりする

## AWS マネージメントコンソールより　インスタンスを作成しようとしています。しかし「インスタンスの詳細の設定」で AZ(アベイラビリティ・ゾーン) を選択するオプションが見当たりません。どのように指定するのでしょうか

- わからん: ウ． VPCおよびサブネットを選択することにより AZ を選択する。
  - インスタンスを生成するときに、VPC 及び サブネットを選択します。サブネットは必ず一つのAZに関連付けられているため、結果としてAZを選択することとなります。

## わからん: "Trusted Advisor"により推奨されるアクションで以下が表示されました。『特定のポートに対して無制限アクセス(0.0.0.0/0)を許可しているセキュリティグループのルールをチェックします。3個中3個のセキュリティグループのルールは、特定のポートに対して無制限アクセスを許可しています。』どういう意味でしょうか

- イ． セキュリティグループでソースが any(0.0.0.0) を許可しているポートがある。IPアドレスのレンジを限定すべきである。
  - 以下のように接続元を any (0.0.0.0)にしているポートが存在します。以下の例では Windows リモートデスクトップ(TCP/3389) の接続がどのIPからも許可されています。
もし管理クライアント(リモートデスクトップクライアント)のIPアドレスが固定しているなら、限定すべきです。

## "Trusted Advisor"により推奨されるアクションで以下が表示されました。『ルートアカウントのMFA このチェックではルートアカウントでMFAが有効にされていない場合にアラートを表示します。このルートアカウントでは、MFAが有効にされておりません。』どういう意味でしょうか

- ウ． AWS アカウントで MFA が有効化されていない。

## AWS アカウントのセキュリティ強化機能である MFA(多要素認証) で可能なオプションは何でしょうか。正しいものをすべて選択してください

- ア． SMS テキストメッセージベース
- ウ． セキュリティトークン

## あなたはアプリケーション層で AWS Auto Scaling で稼働している Amazon EC2 インスタンス のタイプを変更しようとしています。インスタンスの定義を変更すべきなのは次のどれでしょうか

- ア． Auto Scaling Launch Configuration
  - "Auto Scaling Launch Configuration"では以下を設定することが可能です。
    - AMI
    - インスタンスタイプ
    - キーペア
    - ユーザデータ
    - セキュリティグループ
    - IMAロール
  
## ノード間のレイテンシーを少なくするクラスタ構成を設計するには以下のどの機能を使用するべきでしょうか

- ✗: イ． マルチ AZ 機能
- ア． プレイスメントグループ
  - > プレイスメントグループを作成するときは、グループの次のいずれかの戦略を指定します。
    >
    >?クラスター ? 2 つのインスタンスを単一のアベイラビリティーゾーン内の低レイテンシーグループにクラスタ化する
    >
    >?スプレッド ? 基盤となるハードウェアにインスタンスを分散する
  - プレイスメントグループはインスタンス間の通信速度を高速化する機能。ネットワーク遅延を減らす機能。

## わからん: スマホアプリが GPS を使用して 10秒ごとにデータ送信するシステムがあります。これらのデータをリアルタイムで集計する必要があります。多くのユーザがこのアプリを使用する見込みです。このストリームデータを処理するのに適した AWS サービスは何でしょうか

- エ． Amazon Kinesis
  - 大量のデータストリームを処理するのは Kinesis が向いています。SQSも似たサービスですが処理能力はkinesis の方が上になります。SQSはどちらかというとデータの処理ではなくインスタンス間のコマンドの受け渡しとなります。

## わからん: 次の中でデフォルトで暗号化されるサービスは何でしょうか。正しいものをすべて選んでください

## 高可用性を実現するために複数の AZ(Available Zone) に配置する必要があるサービスはどれでしょうか。正しいものをすべて選択してください

- ア． EC2 インスタンス
- イ． Elastic Load Balancing
- ウ． RDS インスタンス
- ○: カ． ElasticCache
  - 選択肢の中で AZ サービスは「EC2 インスタンス」「Elastic Load Balancing」「RDS インスタンス」「ElasticCache」となります。
  - ユーザ側（使用者側）が可用性のために冗長的に配置する必要があります。片系がダウンした場合は切り替わります。
  - その他はリージョンサービスとなり、可用性は Amazon側の担当となります。

## わからん: EBS-backed (EBSがルーツ)の EC2インスタンスがあります。もしこのインスタンスを停止した場合、 Ephemeral Store (インスタンスストア、 Instance Store) 上のデータはどうなりますか

## 次の中でグローバルサービスであるサービスはどれでしょうか。（要するにAZサービス、リージョンサービスでないもの）正しいものを全て選択してください

- ア． IAM (AWS Identity and Access Management)
- イ． Route 53
- ウ． CloudFront
- エ． WAF
- オ． STS (AWS Security Token Service)
  - すべてグローバルサービス

## Amazon S3 (Simple Storage Service) 内のオブジェクトでPUT可能なオブジェクトの最大サイズはどれですか

- ウ． マルチアップロード機能を使用すると最大で 5TB がアップロード可能。
  - 調べた。マルチアップロード機能を使わないと1回のPUTで5GBまで
  - マルチパートアップロード APIにより大容量のオブジェクトを S3 に効率良くアップロードすることが可能です。オブジェクトを分割してアップロードします。

## わからん: あなたは Web サーバの設計者です。S3 (Simple Storage Service) に静的なデータを保存しようとしています。1秒間に200 もの PUT リクエストが送信される想定です。性能を向上させる方法として有効なものを選択してください

## EC2インスタンスのルートボリュームとして使用しているEBSのスナップショットを取得しました。取得中の状況として正しいものをすべて選択してください

- ア． ステータスは「Pending」と表示される。
- イ． スナップショット取得中も原則としてEC2インスタンスは動作している。
- エ． スナップショットは初回はすべて取得されるが、2回目以後は差分のみ取得される。
- ○: オ． スナップショットは圧縮して保存される

## EC2インスタンスから S3に巨大なファイルをアップロードするシステムがあります。しかしこのアップロードの性能が悪くシステム全体のパフォーマンスが満足できません。ネットワークがボトルネックとなっているのではないと報告を受けています。以下のどの機能を使用すればアップロードの性能が改善する可能性がありますか

- ア． Amazon S3 マルチアップロード機能を使用する。

## Amazon Linux 及び Redhat Linux の EC2 インスタンスを作成した場合、ログインに使用するユーザ名は何になりますか

- イ． ec2-user

## Windows の EC2を初めて起動するときに、EC2に新しくアプリケーションをインストールしたいと思います。インスタンスのどの機能を使用すると効率よくインストールできますか

- ✗: ウ． スタートアップスクリプト
- ア． ユーザデータ
  - ユーザデータ内のスクリプトを実行することが可能 です。ユーザデータにAWS Tools for Windows PowerShell を使用することも可能です。

## Web サイトをホスティングしている ELB (Elastic Load Balancing) にexample.co.jp という名前を解決させようと思います。Route 53 で使用するレコードタイプは何ですか

- ✗: イ． CNAMEレコード
- ウ． ALIASレコード

わからん

## エッジロケーションに配置される機能で正しいものを全て選択してください

エッジロケーションとはリージョンやAZとは全く別のデータセンター。  
グローバルサービス向け。  
AZより多い場所にある。世界中に分散することによりどのよう場所からでもアクセスのスピードを速くする設計で検討する。

- ア． CloudFront
- イ． Route 53
- ○: オ． WAF(Web Application Firewall)

## Amazon RDS を複数のAZ (Multi AZ) 構成にするメリットは何でしょうか

- エ． AZ 障害に対する可用性。
  - RDS の Multi-AZ 機能は可用性の機能です。異なるAZ 上のRDSインスタンスへ自動的に複製され、稼働中のRDSあるいはAZで障害が発生した場合は自動的にフェールオーバーされます。

## コスト重視で 静的コンテンツの Web サーバの構築を考えています。ただし高可用性である必要があります。使用するのは組織内のメンバのみであるため、Webアクセス数はあまり多くありません。コスト重視、高可用性を満たす条件でどのサービスを使用するのが一番最適でしょうか

- ウ． Amazon S3(Simple Storage Service)で静的コンテンツの配布を行う。
  - EC2インスタンスで高可用性を実現するには Multi-AZなどのコストが発生する

## Amazon CloudWatch がサポートする最短のデータの受信、データの集計間隔は次の中のどれでしょうか

- わからん: ア． 1秒

## プライベートサブネット上の EC2インスタンスをインターネット上の機器にネットワークで接続させたいと思います。必要な設定は何でしょうか。必要な事項をすべて選択してください

- ア． パブリックサブネットを作成し、NAT インスタンスを設置する。
- ✗: イ． EC2インスタンスにパブリックIP あるいは Elastic IPアドレスを設定する。
- ○: ウ． NAT インスタンスに Elastic IP アドレスあるいはパブリック IP アドレスを設定する。
- ○: エ． プライベートサブネットのルーティングテーブルに送信先の 0.0.0.0/0 に NAT インスタンスを設定する。
- ○: オ． NAT インスタンスの送信先/元のアドレスのチェックを無効化する。
  - EC2に外部接続用のIPアドレスはアサインしません。その代わりに NAT インスタンスに外部接続用のIPをアサインします。ルーティングテーブルのターゲットをNATインスタンスに修正します。

## CloudWatch の監視項目のことを何と呼びますか

- ✗: ウ． モニタリング
- ○: イ． メトリックス
  - メトリックス(metorics)は"活動を定量化し加工した指標"という意味です。
